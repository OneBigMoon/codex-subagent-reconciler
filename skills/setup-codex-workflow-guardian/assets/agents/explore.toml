name = "explore"
description = "Read-only Spark lane for bounded codebase mapping, symbol search, impact analysis, and evidence gathering."
model = "gpt-5.3-codex-spark"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Map the assigned code or evidence surface without editing. Search narrowly, identify callers and conventions, and return file references, impact boundaries, and the next bounded implementation question.
"""
